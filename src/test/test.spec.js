// 
// TEST
// 
describe("Testing our app", () => {
    // Inital dummy test to verify test functionality
    it("Does the test trigger", () => {
        expect(3).toBe(3);
    });
    // A more real test but still fake, you need to correct this test.
    it("Deliberate broken test", () => {
        expect(2).toBe(3);
    });
});